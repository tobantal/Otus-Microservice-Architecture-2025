apiVersion: apps/v1
kind: Deployment
metadata:
  name: saga-billing-service
  namespace: hw08
spec:
  replicas: 1
  selector:
    matchLabels:
      app: saga-billing-service
  template:
    metadata:
      labels:
        app: saga-billing-service
    spec:
      containers:
      - name: saga-billing-service
        image: tobantal/hw08-saga-billing:latest
        ports:
        - containerPort: 8001
        env:
        - name: DB_HOST
          value: "billing-postgres"
        - name: DB_PORT
          value: "5432"
        - name: DB_NAME
          value: "billing_db"
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: db-secret
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-secret
              key: DB_PASSWORD
        - name: RABBITMQ_HOST
          value: "rabbitmq"
        - name: RABBITMQ_PORT
          value: "5672"
        - name: RABBITMQ_USER
          value: "saga"
        - name: RABBITMQ_PASSWORD
          valueFrom:
            secretKeyRef:
              name: rabbitmq-secret
              key: RABBITMQ_PASSWORD
        readinessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 15
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: saga-billing-service
  namespace: hw08
spec:
  selector:
    app: saga-billing-service
  ports:
  - port: 8001
