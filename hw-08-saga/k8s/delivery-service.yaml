apiVersion: apps/v1
kind: Deployment
metadata:
  name: saga-delivery-service
  namespace: hw08
spec:
  replicas: 1
  selector:
    matchLabels:
      app: saga-delivery-service
  template:
    metadata:
      labels:
        app: saga-delivery-service
    spec:
      containers:
      - name: saga-delivery-service
        image: tobantal/hw08-saga-delivery:latest
        ports:
        - containerPort: 8003
        env:
        - name: DB_HOST
          value: "delivery-postgres"
        - name: DB_PORT
          value: "5432"
        - name: DB_NAME
          value: "delivery_db"
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: db-secret
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-secret
              key: DB_PASSWORD
        - name: RABBITMQ_HOST
          value: "rabbitmq"
        - name: RABBITMQ_PORT
          value: "5672"
        - name: RABBITMQ_USER
          value: "saga"
        - name: RABBITMQ_PASSWORD
          valueFrom:
            secretKeyRef:
              name: rabbitmq-secret
              key: RABBITMQ_PASSWORD
        readinessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 15
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: saga-delivery-service
  namespace: hw08
spec:
  selector:
    app: saga-delivery-service
  ports:
  - port: 8003
