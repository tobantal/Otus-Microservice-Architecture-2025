apiVersion: apps/v1
kind: Deployment
metadata:
  name: idempotent-delivery-service
  namespace: hw09
spec:
  replicas: 1
  selector:
    matchLabels:
      app: idempotent-delivery-service
  template:
    metadata:
      labels:
        app: idempotent-delivery-service
    spec:
      initContainers:
      - name: wait-for-rabbitmq
        image: busybox
        command: ['sh', '-c', 'until nc -z rabbitmq 5672; do echo waiting for rabbitmq; sleep 2; done']
      - name: wait-for-postgres
        image: busybox
        command: ['sh', '-c', 'until nc -z delivery-postgres 5432; do echo waiting for postgres; sleep 2; done']
      containers:
      - name: idempotent-delivery-service
        image: tobantal/hw09-idempotent-delivery:latest
        ports:
        - containerPort: 8003
        env:
        - name: DB_HOST
          value: "delivery-postgres"
        - name: DB_PORT
          value: "5432"
        - name: DB_NAME
          value: "delivery_db"
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: db-secret
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-secret
              key: DB_PASSWORD
        - name: RABBITMQ_HOST
          value: "rabbitmq"
        - name: RABBITMQ_PORT
          value: "5672"
        - name: RABBITMQ_USER
          value: "saga"
        - name: RABBITMQ_PASSWORD
          valueFrom:
            secretKeyRef:
              name: rabbitmq-secret
              key: RABBITMQ_PASSWORD
        readinessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 15
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: idempotent-delivery-service
  namespace: hw09
spec:
  selector:
    app: idempotent-delivery-service
  ports:
  - port: 8003
